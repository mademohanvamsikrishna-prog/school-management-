"""
Schemas package — Pydantic request/response models.

Schemas define the shape of data entering and leaving the API.
They are NOT the same as SQLAlchemy models. Each domain module will
have its own schema file (e.g., schemas/user.py, schemas/student.py).
"""
