# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

> **Installed SDK:** `expo ~54.0.0` (see `package.json`). Do **not** use v57 or later APIs — they may not exist in this version.
